service: collection-hello-world

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  stage: dev

functions:
  hello:
    handler: src/handler.hello
    events:
      - http: GET /
        path: hello
        cors:
          origin: "*"